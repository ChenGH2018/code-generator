bean层：
	根据sql文件生成
控制层：
	增：
		新增
	删除：
		根据一个或多个删除物理
		更加一个或者多个软删除（自定义)
	改：
		根据一个或者多个修改
	查：
		分页条件查询
		条件查询所有
		根据ID查询
业务层：
	同控制层

dao层：
	跟curd的种类差不多，但查询需要将字段和表名生成x.xxx类型以便以后修改成关联查询方便


其他方面：
	1、导入的位置根据输入包名定制（减少复制项目中需要一个一个的导包）
	2、先完成基本的或者想到其他再添加